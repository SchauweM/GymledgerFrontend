import { InjuryFilterPipe } from './injury-filter.pipe';

describe('InjuryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InjuryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
