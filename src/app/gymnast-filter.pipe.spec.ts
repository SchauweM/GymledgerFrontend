import { GymnastFilterPipe } from './gymnast-filter.pipe';

describe('GymnastFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GymnastFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
